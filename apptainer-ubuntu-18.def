Bootstrap: docker
From: ubuntu:18.04

%files
./multi2sim-4.1/bin/m2s /usr/local/bin/m2s

%post
    apt update && apt install -y \
        build-essential \
        automake \
        libtool \
        zlib1g-dev \
        libboost-dev \
        # etc.
    apt clean
    rm -rf /var/lib/apt/lists/*
    # Copia tu aplicación
    #mkdir -p /app
    # Si tienes tu ejecutable local:
    #cp -a ./multi2sim-4.1/bin/m2s /app/
